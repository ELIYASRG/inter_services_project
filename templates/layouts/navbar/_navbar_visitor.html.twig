<nav class="navbar navbar-expand-lg bg-body-tertiary shadow">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('visitor.welcome.index')}}">InterServices VLG</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavb" aria-controls="navbarNavb" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavb">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="#">Qui sommes-nous?</a>
                    </li>
                </ul>
                <ul class="navbar-nav ms-auto">
                    {% if app.user %}
                        {% if is_granted('ROLE_SUPER_ADMIN') %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('admin.home.index')}}">Espace admin</a>
                            </li>
                        {% elseif is_granted('ROLE_ADMIN') %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('admin.home.index')}}">Espace admin</a>
                            </li>
                        {% elseif is_granted('ROLE_USER') %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('user.home.index')}}">Espace privé</a>
                            </li>
                        {% endif %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app.logout')}}">Déconnexion</a>
                            </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('visitor.authentication.login')}}">Connexion</a>
                        </li>
                        <li class="nav-item dropstart">
                            
                            {# <div class="dropdown"> #}
                                <button class="btn btn-success dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    Inscription
                                </button>
                                <ul class="dropdown-menu">
                                    <li>
                                        <button class="dropdown-item" type="button">
                                            <a class="nav-link" href="{{ path('visitor.registration.register')}}">Client</a>
                                        </button>
                                    </li>
                                    <li>
                                        <button class="dropdown-item" type="button">
                                            <a class="nav-link" href="{{ path('visitor.registration.associate.register')}}">Associer</a>
                                        </button>
                                    </li>
                                </ul>
                            {# </div> #}
                        </li>
                    {% endif %}
                </ul>
                </div>
            </div>
        </nav>